meta {
  name: Reset Password
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/v1/auth/reset-password
  body: json
  auth: none
}

body:json {
  {
    "token": "REPLACE_WITH_TOKEN_FROM_EMAIL",
    "password": "newpassword123"
  }
}
