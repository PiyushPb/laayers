meta {
  name: Get Settings
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/workspaces/:workspaceId/settings
  body: none
  auth: bearer
}

params:path {
  workspaceId: {{workspaceId}}
}

auth:bearer {
  token: {{accessToken}}
}
