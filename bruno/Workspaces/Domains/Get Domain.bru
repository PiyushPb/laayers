meta {
  name: Get Domain
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/workspaces/:workspaceId/domains/:domainId
  body: none
  auth: bearer
}

params:path {
  workspaceId: {{workspaceId}}
}

auth:bearer {
  token: {{accessToken}}
}

params:path {
  domainId: 
}
