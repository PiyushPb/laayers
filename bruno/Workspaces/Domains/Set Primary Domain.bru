meta {
  name: Set Primary Domain
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/workspaces/:workspaceId/domains/:domainId/set-primary
  body: json
  auth: bearer
}

params:path {
  workspaceId: {{workspaceId}}
  domainId: w2yrqru5rkb73qoczlee51n9
}

auth:bearer {
  token: {{accessToken}}
}
