meta {
  name: Delete Payment Method
  type: http
  seq: 1
}

delete {
  url: {{baseUrl}}/api/v1/workspaces/:workspaceId/billing/payment-methods/:id
  body: none
  auth: bearer
}

params:path {
  workspaceId: {{workspaceId}}
}

auth:bearer {
  token: {{accessToken}}
}

params:path {
  id: 
}
