meta {
  name: Get Invoice
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/workspaces/:workspaceId/billing/invoices/:invoiceId
  body: none
  auth: bearer
}

params:path {
  workspaceId: {{workspaceId}}
}

auth:bearer {
  token: {{accessToken}}
}

params:path {
  invoiceId: 
}
